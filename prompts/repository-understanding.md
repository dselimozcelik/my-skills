# Repository Understanding Prompt

Use the `engineering-mentor` skill in repository-understanding mode. This is read-only: do not implement or edit product files.

Load my learner profile, this repository's local memory, and any active session. Scan the repository in layers, trace representative end-to-end flows from real code and tests, and separate evidence from inference.

Build a dependency-aware concept map with `required now`, `required soon`, and `later` priorities. Compare it with my demonstrated mastery. Discover the broad map, but invoke `teach` only for the smallest concept currently blocking the selected flow. Ask one question at a time and checkpoint repository understanding so a new Windsurf or Copilot chat can continue.
