# Task Start Prompt

Use the `engineering-mentor` skill for this task. Load my learner profile, this repository's local memory, and any active session before starting.

Do not implement immediately. Establish current behavior, desired behavior, acceptance criteria, out-of-scope behavior, relevant flows, risks, and validation. Split the work into semantic blocks that each change one coherent behavior or boundary.

For each block use: orient → invoke `teach` for a blocking concept → understanding check → my explicit approval → focused implementation → actual diff review → my explain-back → focused verification and its limits → memory checkpoint.

Do not teach from the parent skill and do not proceed while the teach result is `in-progress`. Stop for unexpected scope, dependencies, migrations, external actions, branch changes, commits, pushes, merges, or pull requests and request fresh approval.

Report Delivery and Learning separately.
